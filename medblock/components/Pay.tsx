import { View, Text } from "react-native";
import React from "react";

const Pay = ({ data }: { data: string }) => {
  return (
    <View>
      <Text>Pay</Text>
    </View>
  );
};

export default Pay;
